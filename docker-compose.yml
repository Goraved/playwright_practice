version: '3'
services:
  tests:
    image: playw
    command: npm test
    depends_on:
      - setup
    volumes:
      - ./test:/app/test
      - ./allure-results:/app/allure-results
      - ./allure-report:/app/allure-report
  setup:
    build:
      context: .
      dockerfile: Dockerfile
    image: playw